# 🏗️ SaaS Template Architecture Diagram

> **Visual guide to the premium B2B SaaS template structure and data flow**

---

## 📐 Component Hierarchy

```
┌─────────────────────────────────────────────────────────┐
│              Root Layout (layout.tsx)                    │
│         ├── ThemeProvider (Dark/Light Mode)             │
│         └── ScrollProvider (Scroll Context)              │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
   ┌────▼────┐      ┌────▼────┐      ┌────▼────┐
   │  Header │      │   Page  │      │  Footer │
   │ (sticky)│      │ (main)  │      │         │
   │         │      │         │      │ 4 cols  │
   └─────────┘      │         │      └─────────┘
                    │         │
                    │◄────────┤
                    │ Sections │
                    │         │
        ┌───────────┼─────────┴────────────┬─────┬────┐
        │           │                      │     │    │
        │      ┌────▼────┐          ┌──────▼─┐  │    │
        │      │   Hero  │          │Features│  │    │
        │      ├─ 3D (opt)          │ Grid   │  │    │
        │      │ - Orbs  │          └────────┘  │    │
        │      │ - Waves │                      │    │
        │      └─────────┘                      │    │
        │                                       │    │
    ┌───▼──┐  ┌──────────┐  ┌─────────┐  ┌─────▼─┐ │
    │Pricing│  │Testimonials│  │CaseStudies│  │FAQ │ │
    │3 tiers│  │ Carousel   │  │3 stories  │  │    │ │
    └───────┘  └──────────┘  └─────────┘  └─────┘ │
                                                   │
                                              ┌────▼──┐
                                              │  CTA  │
                                              └───────┘
```

---

## 🔄 Data Flow Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Data Sources                          │
├─────────────────────────────────────────────────────────┤
│  features.json  pricing.json  testimonials.json  etc   │
└────┬────────────┬────────────┬────────────────────────┘
     │            │            │
     │  ┌─────────▼────────────▼──────────┐
     │  │  Component Props Mapping         │
     │  │  (Sections use JSON data)        │
     │  └─────────┬────────────┬──────────┘
     │            │            │
┌────▼────┐  ┌────▼────┐  ┌────▼────┐
│Features  │  │Pricing  │  │Testimonials
│Component │  │Component│  │Component
│          │  │         │  │
│maps 6    │  │maps 3   │  │maps 5
│features  │  │tiers    │  │items
│from JSON │  │from JSON│  │from JSON
└────┬─────┘  └────┬────┘  └────┬────┘
     │             │            │
     │      ┌──────▼────────────▼────┐
     │      │   Rendered Components   │
     │      │   (DOM + Animations)    │
     │      └────────────────────────┘
     │
     └──► User sees final page
```

---

## 🌗 Theme System Architecture

```
┌──────────────────────────────────┐
│   useTheme() Hook                │
├──────────────────────────────────┤
│  state: theme ('dark'|'light')   │
│  - localStorage.getItem('theme') │
│  - System preference detection   │
│  - Toggle function               │
└────┬──────────────┬──────────────┘
     │              │
┌────▼────┐    ┌────▼─────┐
│ CSS Var │    │HTML Attr │
│ --color │    │data-theme│
│ updates │    │='dark'   │
└────┬────┘    └────┬─────┘
     │              │
     │              ▼
     └─► Applied to DOM
         ├── Background colors
         ├── Text colors
         ├── Border colors
         └── All CSS variables
```

---

## 📱 Responsive Layout Flow

```
Mobile (320px - 640px)
├── 1-column layout
├── Full-width sections
├── Hamburger menu
├── Stacked CTAs
└── Touch-optimized buttons (48px+)

    ▼ Media query: 768px

Tablet (641px - 1024px)
├── 2-column layouts (where applicable)
├── Wider padding
├── Desktop menu (if space)
└── Optimized spacing

    ▼ Media query: 1024px

Desktop (1025px+)
├── 3-column grids
├── Max-width container (1280px)
├── Full desktop nav
├── Optimized whitespace
└── Full animations enabled
```

---

## 🎬 Animation Architecture

```
┌─────────────────────────────────────┐
│  Framer Motion Integration          │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────────────────────┐   │
│  │  Scroll-Triggered Animations │   │
│  ├──────────────────────────────┤   │
│  │  useScrollAnimation Hook      │   │
│  │  ├─ Intersection Observer     │   │
│  │  ├─ Returns: isInView boolean│   │
│  │  └─ Triggers: whileInView    │   │
│  └────┬─────────────────────────┘   │
│       │                              │
│  ┌────▼─────────────────────────┐   │
│  │  Entrance Effects             │   │
│  ├──────────────────────────────┤   │
│  │  initial={{ opacity: 0 }}    │   │
│  │  whileInView={{ opacity: 1 }}│   │
│  │  transition={{ duration: 0.6 }}  │
│  └──────────────────────────────┘   │
│                                     │
│  ┌──────────────────────────────┐   │
│  │  Hover & Tap Effects         │   │
│  ├──────────────────────────────┤   │
│  │  whileHover={{ scale: 1.05 }}│   │
│  │  whileTap={{ scale: 0.95 }}  │   │
│  │  (On buttons, cards, etc)    │   │
│  └──────────────────────────────┘   │
│                                     │
│  ┌──────────────────────────────┐   │
│  │  Auto Animations             │   │
│  ├──────────────────────────────┤   │
│  │  animate={{ rotation: 360 }} │   │
│  │  transition={{ repeat: Inf }}│   │
│  │  (Testimonial carousel)      │   │
│  └──────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
          │
          ▼
      GPU-accelerated rendering
      (60fps target)
```

---

## 🎨 Styling Architecture

```
┌──────────────────────────────────┐
│  Styling Layers (Priority Order) │
├──────────────────────────────────┤
│                                  │
│  1. Component Styles             │
│     └─ CSS Modules (.module.css) │
│                                  │
│  2. Tailwind Classes             │
│     ├─ Utility classes           │
│     ├─ Dark mode: dark:prefix    │
│     └─ Responsive: sm:/md:/lg:   │
│                                  │
│  3. CSS Variables                │
│     ├─ --color-primary           │
│     ├─ --color-background        │
│     └─ Updated by useTheme hook  │
│                                  │
│  4. Base Styles                  │
│     ├─ globals.css               │
│     ├─ Resets & typography       │
│     └─ Animations & utilities    │
│                                  │
└──────────────────────────────────┘
          │
          ▼
    Computed Styles
    Applied to DOM
```

---

## 🔗 State Management Flow

```
┌─────────────────────────────────┐
│      Global State               │
├─────────────────────────────────┤
│                                 │
│  ┌──────────────────────────┐   │
│  │  ThemeContext            │   │
│  │  ├─ theme: 'dark'|'light'│   │
│  │  ├─ toggleTheme()        │   │
│  │  └─ localStorage sync    │   │
│  └─────────┬────────────────┘   │
│            │                     │
│  ┌─────────▼────────────────┐   │
│  │  ScrollContext           │   │
│  │  ├─ scrollY position     │   │
│  │  ├─ scrollProgress [0-1] │   │
│  │  └─ Real-time updates   │   │
│  └──────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│      Local Component State       │
├─────────────────────────────────┤
│  ├─ Mobile menu (open/close)    │
│  ├─ Testimonial slide index     │
│  ├─ Pricing toggle (monthly/yr) │
│  ├─ FAQ expanded state          │
│  └─ Scroll animation triggers   │
└─────────────────────────────────┘
```

---

## 🔀 Custom Hook Architecture

```
┌─────────────────────────────┐
│    useTheme()               │
├─────────────────────────────┤
│ Returns:                    │
│  - theme: 'dark'|'light'   │
│  - toggleTheme: () => void │
│                             │
│ Uses:                       │
│  - localStorage             │
│  - CSS variable update      │
│  - System preference        │
└────┬────────────────────────┘
     │
┌────▼────────────────────────┐
│    useScrollAnimation()      │
├─────────────────────────────┤
│ Returns: isInView boolean   │
│                             │
│ Uses:                       │
│  - Intersection Observer    │
│  - useRef for element ref   │
│  - useEffect for lifecycle │
└────┬────────────────────────┘
     │
┌────▼────────────────────────┐
│    useMobileMenu()           │
├─────────────────────────────┤
│ Returns:                    │
│  - isOpen: boolean          │
│  - toggle/open/close funcs │
│                             │
│ Uses:                       │
│  - useState for menu state │
└─────────────────────────────┘
```

---

## 📦 3D Integration Architecture (Optional)

```
┌──────────────────────────────────┐
│  HeroSection Component           │
├──────────────────────────────────┤
│  ├─ enable3D = true|false        │
│  ├─ variant3D = 'orbs'|'waves'   │
│  └─ Conditionally renders:       │
│     └─ <HeroCanvas />            │
└────┬─────────────────────────────┘
     │
┌────▼─────────────────────────────┐
│  HeroCanvas.tsx (R3F Wrapper)    │
├──────────────────────────────────┤
│  <Canvas                         │
│    dpr={1.5}                     │
│    gl={{ alpha: true }}          │
│    camera={{ position: [0,0,30] }}
│  >                               │
│    <Lights /> (2: ambient+dir)  │
│    <Suspense>                    │
│      <AnimatedOrbs|Waves />     │
│    </Suspense>                   │
│  </Canvas>                       │
└────┬─────────────────────────────┘
     │
     ├──────────────────┬──────────────────┐
     │                  │                  │
┌────▼──────────┐  ┌────▼──────────┐    │
│AnimatedOrbs.tsx    │AnimatedWaves.tsx    │
├────────────────    ├────────────────     │
│4 Spheres:         │Dual mesh layers:    │
│  - Indigo          │  - Layer 1: indigo  │
│  - Violet          │  - Layer 2: violet  │
│  - Pink            │  - Vertex animation │
│  - Cyan            │  - Transparent      │
│                    │                     │
│Orbital motion:     │Wave calculation:    │
│  - useFrame        │  - Math.sin/cos     │
│  - Emissive glow   │  - Position updates │
│  - Pulsing         │  - GPU-accelerated  │
└────────────────┘  └────────────────┘
     │                     │
     └─────────┬───────────┘
               │
               ▼
         WebGL Rendering
         (Canvas element)
         │
         ▼
     Positioned behind
     Hero content overlay
```

---

## 🚀 Build & Deploy Pipeline

```
Development
├─ npm install
├─ npm run dev
└─ localhost:3000

    ▼

Code Changes
├─ Edit components
├─ Update JSON data
└─ Change theme

    ▼

Testing
├─ npm run lint (ESLint)
├─ npm run type-check (TypeScript)
└─ Manual browser testing

    ▼

Build
├─ npm run build
├─ Next.js optimization
└─ Static/dynamic routes

    ▼

Deployment
├─ Option 1: vercel (recommended)
├─ Option 2: Docker
└─ Option 3: Self-hosted (npm start)

    ▼

Production
├─ Custom domain
├─ SSL certificate
├─ CDN caching
└─ Analytics tracking
```

---

## 🎯 Content Management Flow

```
Editor Updates Content
│
├─ Edit JSON file
│  └─ src/data/features.json
│
├─ Save changes
│  └─ File system update
│
├─ HMR detects change
│  └─ Hot Module Replacement
│
├─ Component re-renders
│  └─ New data from JSON
│
└─ User sees updates
   └─ No code changes needed
```

---

## 🔐 Security & Performance Layers

```
┌──────────────────────────────┐
│  Browser (Client-Side)       │
├──────────────────────────────┤
│  - CSP headers               │
│  - X-Frame-Options           │
│  - X-Content-Type-Options    │
└──────────────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  Next.js (Framework)         │
├──────────────────────────────┤
│  - Static optimization       │
│  - Code splitting            │
│  - Compression (gzip)        │
└──────────────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  TypeScript (Type Safety)    │
├──────────────────────────────┤
│  - Strict mode               │
│  - No implicit any           │
│  - Full type checking        │
└──────────────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  Static Site (No API)        │
├──────────────────────────────┤
│  - No backend to attack      │
│  - No database vulnerabilities
│  - Content delivery only     │
└──────────────────────────────┘
```

---

**This architecture ensures optimal performance, security, and maintainability** ✨

